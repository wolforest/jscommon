{
  "name": "@wolforest/jscommon-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean",
    "publish:beta": "pnpm -r publish --tag beta"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.0.0",
    "vite-plugin-dts": "^4.4.0",
    "vitest": "^1.2.2"
  }
}
